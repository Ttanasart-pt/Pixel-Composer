Cache non-surface values in to array.

## Properties

[proptable]
Range
Start Frame|The first frame to cache (set to -1 to cache from the first frame).
Stop Frame|The last frame to cache (set to -1 to cache to the last frame).
[/proptable]